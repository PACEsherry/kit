<!-- source: clients\typescript\.eslintrc.js -->

# `clients\typescript\.eslintrc.js`

---

## function:

此配置文件为TypeScript项目定义ESLint规则，控制代码静态检查与质量保证。关键配置包括使用TypeScript解析器、扩展推荐规则集、并针对项目需求调整特定规则（如允许any类型、忽略未使用变量警告、允许require语法）。这些设置直接影响代码提交前的检查和持续集成流程，确保代码符合团队规范且避免不必要的编译或运行时错误。

## declaration:

```js
module.exports = {
  root: true,
  parser: "@typescript-eslint/parser",
  plugins: ["@typescript-eslint"],
  extends: ["eslint:recommended", "plugin:@typescript-eslint/recommended"],
  env: {
    es2021: true,
    node: true,
    jest: true,
  },
  ignorePatterns: ["dist/", "node_modules/"],
  rules: {
    "@typescript-eslint/no-explicit-any": "off",
    "@typescript-eslint/no-unused-vars": ["warn", { argsIgnorePattern: "^_" }],
    "@typescript-eslint/no-var-requires": "off",
    "@typescript-eslint/no-require-imports": "off",
    "no-empty": ["warn", { allowEmptyCatch: true }],
  },
};
```
